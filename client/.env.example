VITE_GOOGLE_CLIENT_ID=
# Set this to the public HTTPS API origin when frontend and backend are deployed separately.
# Leave it empty when the production web server proxies /api to the backend.
VITE_API_BASE_URL=
# Development only: Vite's local proxy target.
VITE_API_PROXY_TARGET=http://127.0.0.1:5000
